import{StyleSheet } from 'react-native'

export default StyleSheet.create ({ //Criar a folha de stilo 
    ex: { //Classe que contem as propriedades para o componente
        paddingHorizontal: 15,
        marginVertical: 5,
        borderRadius: 10,
        borderColor: 2,
        borderColor: '#222',
        fontSize: 24,
        fontWeight: 'bold',
    }
})